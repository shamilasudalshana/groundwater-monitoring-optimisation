# Copy this file to ".env" (same folder) and fill in real values.
# .env is git-ignored — your password never gets committed.

# Project root on the network drive (this repo's working data lives under it)
PROJECT_FOLDER=P:/2025/250129_Panzenberg_WF_Messnetzoptimierung_Vorauswahl/40_GIS/44_Scrips/SHe_optimisation_v2

# PostgreSQL / PostGIS connection (hydro_db)
PGHOST=localhost
PGPORT=5432
PGDATABASE=hydro_db
PGUSER=geodin2pg_etl_user
PGPASSWORD=changeme
